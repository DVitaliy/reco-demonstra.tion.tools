UnauthorizedError:
  description: Unauthorized
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/Error"

BadRequestError:
  description: Bad request
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/Error"

NotFoundError:
  description: Resource not found
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/Error"
